# Artemis Review Ticket

Review the ticket context and current diff.

Required:

```bash
git status --short
git diff --stat
git diff
python -m pytest -q
pga validate-registries
pga validate-work-items
```
