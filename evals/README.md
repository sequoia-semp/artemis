# Evals

This folder contains invariant and regression specifications. Executable pytest equivalents currently live in `tests/`.

Every behavior-changing change should add or update both:

- a human-readable eval spec under `evals/`
- executable tests under `tests/`
