# Examples

This directory may hold reviewed examples for onboarding, tests, and agent context.

Examples are not authoritative by themselves. If an example implies behavior, that behavior must be backed by a locked convention, registry, schema, deterministic service, or test.
