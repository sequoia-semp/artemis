# Market Conventions

This directory may summarize approved conventions, but it cannot define new conventions.

Authoritative convention sources remain `docs/CONVENTIONS_LOCKED_v0.1.md`, `domain/`, registries, schemas, and tests. Any convention change requires the change-control path in `development/CHANGE_POLICY.md`.
