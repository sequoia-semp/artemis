legacy: true
legacy_replacement: local/artemis.local.example.yaml
legacy_note: >
  Retained only so older file references fail informatively. Current Artemis
  local configuration uses local/artemis.local.example.yaml and untracked
  local/artemis.local.yaml.
